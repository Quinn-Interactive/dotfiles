alias  _='doas'
alias zl='zfs list -o name,creation,used,available,reservation,canmount,mounted,mountpoint,compressratio,compression'
alias zla='zfs list -o name,creation,used,available,reservation,canmount,mounted,mountpoint,compressratio,compression -t all'
alias zls='zfs list -o name,creation,used,available,reservation,canmount,mounted,mountpoint,compressratio,compression -t snapshot'
